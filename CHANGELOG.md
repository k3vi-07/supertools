# 更新日志

## 1.5.2 - 2026-09-04

### 安全

- 将远程组件缓存键改为经 URL 白名单校验后的 SHA-256 文件名，阻止 Windows 反斜杠目录穿越。
- 移除全局 `no-sandbox` 与 `disable-gpu-sandbox`，恢复 Electron Chromium 沙盒。
- 缩减隔离远程工具窗口的 preload 能力，只保留组件运行所需接口。
- 远程 registry 优先绑定不可变 commit SHA，显式版本安装失败时不再静默回退到其他分支。

### 修复

- 阻止不同仓库的同 ID 工具互相覆盖，并按仓库隔离安装、卸载、评分和更新状态。
- 批量更新失败项继续保留为待更新；仓库检查失败不再显示为“全部最新”。
- 导入列表仅允许恢复用户已添加仓库中的工具。

### 体验

- 工具卡改用原生按钮语义，补齐键盘操作与全局可见焦点。
- 确认弹窗增加焦点环路、Esc 关闭和关闭后的焦点恢复。
- 提升深浅主题三级文字对比度，并修复 900×600 最小窗口下商店响应式断点与触控区域。

### 验证

- 98 项自动化测试、136 个内置工具、134 个社区插件、21 个加密实现契约、类型检查、生产构建与 Electron 运行时视觉验收通过。

## 1.5.1 - 2026-08-30

### 修复

- 修复 macOS 自动更新下载后的 App bundle 缺少完整资源封装签名，导致 ShipIt 报“代码不含资源，但签名指示这些资源必须存在”。
- 将 ad-hoc 签名移入 electron-builder 的 `afterPack` 阶段，确保 DMG/ZIP 生成前完成嵌套组件和 App bundle 签名。
- 发布流水线增加完整质量检查、单次 Release 汇总、版本一致性校验与 SHA-256 清单。

## 1.5.0 - 2026-08-30

### 新增

- 插件库新增 XChaCha20-Poly1305、AES-GCM-SIV 与 AES Key Wrap。
- 远程插件运行时加入 `@noble/ciphers` 受信模块映射。
- 加密插件增加标准向量、往返与密文篡改拒绝测试。

### 修复

- 统一编码、解码及加密插件的双向切换、复制、输入校验和移动端交互。
- 将伪实现或简化实现替换为可审计的标准库实现，并标记兼容算法与非加密哈希。
- 修复远程插件加载、注册表校验、缓存、更新与隔离窗口相关问题。

### 验证

- 注册表、工具清单、加密实现契约、类型检查、单元测试和生产构建纳入发布检查。
